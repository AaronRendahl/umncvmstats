---
output: github_document
---

<!-- README.md is generated from README.Rmd. Please edit that file -->

```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>",
  fig.path = "man/figures/README-",
  out.width = "100%"
)
```

# umncvmstats

<!-- badges: start -->
<!-- badges: end -->

The goal of umncvmstats is to ...

## Installation

You can install the development version of umncvmstats from [GitHub](https://github.com/) with:

``` r
# install.packages("devtools")
devtools::install_github("AaronRendahl/umncvmstats")
```

## Example

This is a basic example which shows you how to solve a common problem:

```{r example}
library(umncvmstats)
mtcars <- datasets::mtcars |> 
  dplyr::mutate(am=forcats::fct_recode(factor(am), 
                    "automatic"="0", "manual"="1"))

combine_tests(
  one_t_test(wt ~ factor(am), mtcars),
  two_t_test(wt ~ factor(am), mtcars))
```
